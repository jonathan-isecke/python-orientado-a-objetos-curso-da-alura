"""
O projeto ByteBank

> Nesta aula, criamos um Ambiente Virtual. Até o momento, não havia um Ambiente Virtual instanciado no diretório.
> Criar um 'Virtual Environment' é interessante para testarmos aplicações e também para não criarmos conflitos com
diferentes projetos que já estão na nossa máquina.
> A prática da criação do arquivo 'requirements.txt' dentro de ambientes virtuais acaba por dinamizar a replicação de
um mesmo ambiente de projeto em máquinas diferentes. (Para isso, executar no terminal: pip install -r requirements.txt)
> O uso de ambientes virtuais acaba evitando conflitos entre versões de pacotes utilizadosem diferentes objetos pois
cada ambiente pode ter uma versão diferente instalada do mesmo pacote/biblioteca/extensão isoladamente de qualquer outro
projeto.
> O Teste verifica se cada método/classe está se comportando como deveria



Para isso, vamos executar no nosso terminal:

    python3 -m venv venv  # O primeiro 'venv' significa 'Virtual environment'. O segundo, é o nome que estamos dando ao
                            ambiente que acabamos de criar. Nesse caso, também é 'venv', mas pode ser qualquer coisa.

    source venv/bin/activate # (Linux)
    venv\Scripts\activate # (Windows)

OBS: Para mim, executar no terminal escrevendo 'python3' não funcionou. Substituí por 'python' e mantive o restante.
Deu certo!

Para esta atividade, criamos o arquivo um arquivo no seguinte diretório: 'projeto-do-bytebank/codigo/main.py'

(PASSO 1): No arquivo 'main.py' do diretório acima.
"""
